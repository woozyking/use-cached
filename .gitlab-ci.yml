image: node:11

cache:
  paths:
    - node_modules
    - .yarn-cache

stages:
  - test

test:
  stage: test
  before_script:
    - yarn config set cache-folder .yarn-cache
    - yarn install
  script:
    - yarn lint
    - yarn test --coverage
    - yarn build
